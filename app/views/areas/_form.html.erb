<%= form_for(@area) do |f| %>
  <% if @area.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@area.errors.count, "error") %> prohibited this area from being saved:</h2>

      <ul>
      <% @area.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <hr>
  <div class="form-group">
        <P ALIGN=right><%= f.label :tipo_area, class: "col-sm-2 col-sm-2 control-label" %>
        <div class="col-sm-4">
            <%=f.text_field :tipo_area, class: "form-control" %>
            <br>
            <br>
        </div>
    </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
