
<%= form_for(@course) do |f| %>
  <% if @course.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@course.errors.count, "error") %> prohibited this course from being saved:</h2>

      <ul>
      <% @course.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <hr>
   <div class="form-group">
        <P ALIGN=right><%= f.label :nombre_curso, class: "col-sm-2 col-sm-2 control-label" %>
        <div class="col-sm-4">
            <%=f.text_field :nombre_curso, class: "form-control" %>
            <br>
            <br>
        </div>
    </div>
   <div class="form-group">
        <P ALIGN=right><%= f.label :area_id, class: "col-sm-2 col-sm-2 control-label" %>
        <div class="col-sm-4">
            <%=f.text_field :area_id, class: "form-control" %>
            <br>
            <br>
        </div>
    </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
